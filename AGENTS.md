# Project collaboration instructions

- After every completed implementation or project change, include ready-to-copy Windows CMD commands in the final response for staging, committing, and pushing the completed work to GitHub.
- Base the commands on the repository's actual current branch and remote. Use a concise commit message that describes the completed change.
- Do not execute the commit or push unless the user explicitly asks Codex to do it.
